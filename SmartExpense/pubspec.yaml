name: smart_expense
description: "Smart Expense Tracker & Currency Converter"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # State Management
  provider: ^6.1.5

  # HTTP Request
  http: ^1.5.0

  # Local Storage
  shared_preferences: ^2.5.3

  # Formatting
  intl: ^0.20.2

  # Generate Unique ID
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true